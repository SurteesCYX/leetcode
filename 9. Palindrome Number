class Solution {
    public boolean isPalindrome(int x) {
        if(x<0){return false;}
        int temp=x;
        int result=0;
        while(x/10>0){
            result=result*10+x%10;
            x=x/10;
        }
        result=result*10+x;
        System.out.println(result);
        return result==temp;
        //if(result==x)
    }
}
Runtime: 38 ms, faster than 70.95% of Java online submissions for Palindrome Number.
Memory Usage: 37 MB, less than 100.00% of Java online submissions for Palindrome Number.
